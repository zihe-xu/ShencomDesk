+    Closed,
+}
+
+impl fmt::Display for EventReceiveError {
+    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result {
+        match self {
+            Self::Lagged(skipped) => write!(formatter, "event subscriber lagged by {skipped} messages"),
+            Self::Closed => formatter.write_str("event bus is closed"),
+        }
+    }
+}
+
+impl Error for EventReceiveError {}
+
+impl From<broadcast::error::RecvError> for EventReceiveError {
+    fn from(error: broadcast::error::RecvError) -> Self {
+        match error {
+            broadcast::error::RecvError::Closed => Self::Closed,
+            broadcast::error::RecvError::Lagged(skipped) => Self::Lagged(skipped),
+        }
+    }
+}
+
+fn unix_time_ms() -> u64 {
+    let milliseconds = SystemTime::now()
+        .duration_since(UNIX_EPOCH)
+        .unwrap_or_default()
+        .as_millis();
+    milliseconds.min(u128::from(u64::MAX)) as u64
+}
+
+#[cfg(test)]
+mod tests {
+    use super::*;
+
+    #[test]
+    fn fans_out_the_same_envelope_to_multiple_subscribers() {
+        tauri::async_runtime::block_on(async {
+            let bus = EventBus::new(8);
+            let mut first = bus.subscribe();
+            let mut second = bus.subscribe();
+
+            let published = bus.publish(AppEvent::ApplicationReady);
+            let first_event = first.recv().await.expect("first subscriber should receive");
+            let second_event = second
+                .recv()
+                .await
+                .expect("second subscriber should receive");
+
+            assert_eq!(first_event, published);
+            assert_eq!(second_event, published);
+            assert_eq!(bus.subscriber_count(), 2);
+        });
+    }
+
+    #[test]
+    fn filtered_subscriber_only_returns_selected_kinds() {
+        tauri::async_runtime::block_on(async {
+            let bus = EventBus::new(8);
+            let mut subscriber = bus.subscribe_to([EventKind::UpdateAvailable]);
+
+            bus.publish(AppEvent::ApplicationReady);
+            bus.publish(AppEvent::UpdateAvailable {
+                version: "2.0.0".to_owned(),
+            });
+
+            let received = subscriber.recv().await.expect("matching event should arrive");
+            assert_eq!(received.event.kind(), EventKind::UpdateAvailable);
+        });
+    }
+
+    #[test]
+    fn reports_lag_for_a_slow_subscriber() {
+        tauri::async_runtime::block_on(async {
+            let bus = EventBus::new(2);
+            let mut subscriber = bus.subscribe();
+
+            bus.publish(AppEvent::ApplicationReady);
+            bus.publish(AppEvent::ApplicationExiting);
+            bus.publish(AppEvent::UpdateAvailable {
+                version: "2.0.0".to_owned(),
+            });
+
+            assert_eq!(subscriber.recv().await, Err(EventReceiveError::Lagged(1)));
+        });
+    }
+
+    #[test]
+    fn cloned_publishers_share_a_monotonic_sequence() {
+        let bus = EventBus::new(4);
+        let clone = bus.clone();
+
+        assert_eq!(bus.publish(AppEvent::ApplicationReady).sequence, 1);
+        assert_eq!(clone.publish(AppEvent::ApplicationExiting).sequence, 2);
+    }
+
+    #[test]
+    fn publishing_without_subscribers_is_valid() {
+        let bus = EventBus::new(4);
+        let envelope = bus.publish(AppEvent::ApplicationReady);
+
+        assert_eq!(envelope.sequence, 1);
+        assert_eq!(bus.subscriber_count(), 0);
+    }
+}
+diff --git a/apps/desktop/src-tauri/src/application/mod.rs b/apps/desktop/src-tauri/src/application/mod.rs
+index 451549d..973d088 100644
+--- a/apps/desktop/src-tauri/src/application/mod.rs
++++ b/apps/desktop/src-tauri/src/application/mod.rs
+@@ -2,6 +2,7 @@
+ 
+ pub mod config_repository;
+ pub mod config_service;
++pub mod event_bus;
+ pub mod task_service;
+ 
+ pub mod health_service {
+diff --git a/apps/desktop/src-tauri/src/application/task_service.rs b/apps/desktop/src-tauri/src/application/task_service.rs
+index b990ef5..0edf356 100644
+--- a/apps/desktop/src-tauri/src/application/task_service.rs
++++ b/apps/desktop/src-tauri/src/application/task_service.rs
+@@ -11,7 +11,13 @@ use std::{
+ 
+ use tauri::async_runtime::{channel, spawn, spawn_blocking, Mutex as AsyncMutex, Receiver, Sender};
+ 
+-use crate::domain::task::{TaskId, TaskSnapshot, TaskState};
++use crate::{
++    application::event_bus::EventBus,
++    domain::{
++        event::AppEvent,
++        task::{TaskId, TaskSnapshot, TaskState},
++    },
++};
+ 
+ const DEFAULT_QUEUE_CAPACITY: usize = 128;
+ const DEFAULT_WORKER_COUNT: usize = 2;
+@@ -80,6 +86,7 @@ pub struct TaskContext {
+     id: TaskId,
+     tasks: TaskRecords,
+     cancellation: Arc<AtomicBool>,
++    events: EventBus,
+ }
+ 
+ impl TaskContext {
+@@ -98,18 +105,25 @@ impl TaskContext {
+             return false;
+         }
+ 
+-        let mut tasks = write_records(&self.tasks);
+-        let Some(record) = tasks.get_mut(&self.id) else {
