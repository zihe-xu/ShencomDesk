{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege IPC capability for the ShenDesk main window",
  "windows": ["main"],
  "permissions": [
    "allow-health-check",
    "allow-get-config",
    "allow-save-config",
    "allow-reset-config"
  ]
}
